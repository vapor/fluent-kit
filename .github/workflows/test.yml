name: test
on:
- pull_request
jobs:
  xenial:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: docker://vapor/swift:5.1-xenial
      with:
        entrypoint: swift
        args: test
  bionic:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: docker://vapor/swift:5.1-bionic
      with:
        entrypoint: swift
        args: test
  sqlite:
    container: 
      image: vapor/swift:5.1
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      with:
        repository: fluent-sqlite-driver
    - run: swift package edit fluent-kit --revision ${{ github.sha }}
    - run: swift test
  postgres:
    container: 
      image: vapor/swift:5.1
    services:
      psql:
        image: postgres
        ports:
          - 5432:5432
        env:
          POSTGRES_USER: vapor_username
          POSTGRES_DB: vapor_database
          POSTGRES_PASSWORD: vapor_password
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      with:
        repository: fluent-postgres-driver
    - run: swift package edit fluent-kit --revision ${{ github.sha }}
    - run: swift test
  mysql:
    container: 
      image: vapor/swift:5.1
    services:
      mysql:
        image: mysql
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: true
          MYSQL_DATABASE: vapor_database
          MYSQL_USER: vapor_username
          MYSQL_PASSWORD: vapor_password
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      with:
        repository: fluent-mysql-driver
    - run: swift package edit fluent-kit --revision ${{ github.sha }}
    - run: swift test
      env:
        MYSQL_HOSTNAME: mysql
        MYSQL_TLS: true
